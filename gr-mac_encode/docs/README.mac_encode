This is the mac_encode-write-a-block package meant as a guide to building
out-of-tree packages. To use the mac_encode blocks, the Python namespaces
is in 'mac_encode', which is imported as:

    import mac_encode

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(mac_encode)
